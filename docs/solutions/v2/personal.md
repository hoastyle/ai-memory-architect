# 个人版方案（优化版）

**版本**: v2.0
**日期**: 2026-03-08
**基础**: openclaw_memory_supersystem-v1.0

---

## 🎯 方案定位

**核心决策**: 直接采用 openclaw_memory_supersystem-v1.0，不重新设计

**理由**:
1. ✅ 成熟度高：7385 行代码，v1.0 → v1.6.0 多版本迭代
2. ✅ 完全开源：MIT 许可证
3. ✅ 纯本地化：SQLite 存储，无强制网络依赖
4. ✅ 低硬件需求：纯 CPU 模式可用
5. ✅ 独特价值：7 阶段 Consolidation（记忆整合）

---

## 🏗️ 架构概览

### 三层记忆架构

```
┌─────────────────────────────────────────┐
│  Layer 1: 工作记忆（2000 tokens）       │
│  - 高频访问的核心记忆                    │
│  - 每次对话自动加载                      │
│  - 存储: SQLite (memory.db)             │
└──────────────┬──────────────────────────┘
               │ 按需检索
               ▼
┌─────────────────────────────────────────┐
│  Layer 2: 长期记忆                       │
│  - Fact: 确定的事实（置信度 0.9-1.0）   │
│  - Belief: 推断的信念（0.3-0.8）        │
│  - Summary: 总结性知识                   │
│  - 自动衰减机制                          │
│  - 存储: SQLite (memory.db)             │
└──────────────┬──────────────────────────┘
               │ 定期整合
               ▼
┌─────────────────────────────────────────┐
│  Layer 3: 原始事件日志                   │
│  - 每日对话记录                          │
│  - 双格式: .md（人读）+ .jsonl（机读）   │
│  - 存储: 文件系统                        │
└─────────────────────────────────────────┘
```

---

## 🔧 两种配置模式

### 模式 1: 轻量模式（推荐）

**适用场景**: 硬件资源受限，优先本地化

**技术栈**:
```
SQLite (内置)
+ 规则引擎（关键词匹配）
+ TF-IDF 检索（可选）
+ 7 阶段 Consolidation
```

**硬件需求**:
- CPU: 2 核
- 内存: 2GB
- 存储: 5GB

**优势**:
- ✅ 零网络依赖
- ✅ 启动速度快（< 1s）
- ✅ 内存占用低（< 300MB）

**劣势**:
- ❌ 无语义理解（纯关键词）
- ❌ 检索精度 ~70%

### 模式 2: 增强模式

**适用场景**: 追求高精度，可接受额外资源

**技术栈**:
```
SQLite
+ 向量检索（v1.6.0）
+ 本地 Ollama Embedding（推荐）
  或 远程 API（Jina/OpenAI）
+ 混合检索（关键词 + 向量）
```

**硬件需求**:
- CPU: 4 核（或 GPU）
- 内存: 4-8GB
- 存储: 10-20GB（Ollama 模型）

**优势**:
- ✅ 语义理解能力
- ✅ 检索精度 > 85%
- ✅ 支持换种说法搜索

**劣势**:
- ❌ 资源占用较高
- ❌ 启动时间较长

---

## 🌟 核心特性

### 1. 7 阶段 Consolidation（记忆整合）

**触发时机**: 20 分钟无消息 + 非活跃时段

**整合流程**:
```
Phase 1: 收集原始对话（Layer 3）
    ↓
Phase 2: 筛选有价值内容（LLM 判断）
    ↓
Phase 3: 提取关键信息
    ↓
Phase 4: 分类（Fact/Belief/Summary）
    ↓
Phase 5: 计算衰减权重
    ↓
Phase 6: 归档到 Layer 2
    ↓
Phase 7: 更新 Layer 1 快照
```

**价值**:
- Token 消耗降低 60-75%
- 自动整理，无需人工维护
- 模拟人脑睡眠时的记忆整理

### 2. Fact/Belief 区分（元认知）

**Fact（事实）**:
- 用户明确说过的内容
- 置信度: 0.9-1.0
- 半衰期: ~87 天
- 使用方式: 直接引用

**Belief（信念）**:
- AI 推断出的内容
- 置信度: 0.3-0.8
- 半衰期: ~10 天
- 使用方式: 谨慎表达，标注不确定性

**示例**:
```
[Fact] 用户是医学生（置信度 1.0）
[Belief] 用户可能对神经科学感兴趣（置信度 0.6）
```

**AI 回复**:
- "你之前提到过你是医学生"（确定语气）
- "我记得你好像对神经科学感兴趣？"（商量语气）

### 3. 自动衰减机制

**衰减公式**:
```python
新权重 = 旧权重 × e^(-衰减率 × 天数)
```

**衰减率**:
| 类型 | 衰减率 | 半衰期 |
|------|--------|--------|
| Fact | 0.008 | ~87 天 |
| Belief | 0.07 | ~10 天 |
| Summary | 0.025 | ~28 天 |
| Event | 0.15 | ~5 天 |

**效果**: 自动遗忘不重要的记忆，保持数据库清洁

---

## 📋 部署指南

### 快速开始

```bash
# 1. 克隆仓库
cd /home/howie/Workspace/Project/Assistant/memory
cd openclaw_memory_supersystem-v1.0

# 2. 安装依赖（轻量模式）
pip install -r requirements.txt

# 3. 初始化
python src/memory.py init

# 4. 配置（可选）
vim config.json

# 5. 启动
python src/memory.py serve
```

### 配置文件示例

```json
{
  "mode": "lightweight",
  "layer1_max_tokens": 2000,
  "consolidation": {
    "enabled": true,
    "idle_minutes": 20,
    "phases": [1, 2, 3, 4, 5, 6, 7]
  },
  "decay": {
    "fact": 0.008,
    "belief": 0.07,
    "summary": 0.025
  },
  "vector_search": {
    "enabled": false
  }
}
```

### 增强模式配置

```json
{
  "mode": "enhanced",
  "vector_search": {
    "enabled": true,
    "provider": "ollama",
    "model": "nomic-embed-text",
    "endpoint": "http://localhost:11434"
  }
}
```

---

## 🔄 工作流程

### 日常使用流程

```
用户启动 OpenClaw
    ↓
加载 Layer 1（2000 tokens）
    ↓
用户输入查询
    ↓
检索 Layer 2（相关记忆）
    ↓
AI 生成回复
    ↓
记录到 Layer 3（当日日志）
    ↓
（空闲时）触发 Consolidation
    ↓
整合到 Layer 2 → 更新 Layer 1
```

---

## 📊 性能指标

| 指标 | 轻量模式 | 增强模式 |
|------|----------|----------|
| 启动时间 | < 1s | < 3s |
| 检索延迟 | < 50ms | < 200ms |
| 内存占用 | < 300MB | < 1GB |
| 检索精度 | ~70% | > 85% |
| Token 消耗 | < 1500/次 | < 1500/次 |
| 网络依赖 | 无 | 可选 |

---

## 💡 最佳实践

### 1. Layer 1 管理
- 保持在 2000 tokens 以内
- 只放最核心、最高频的记忆
- 定期审查和清理

### 2. Consolidation 时机
- 建议在非工作时间运行
- 避免在对话进行时触发
- 可以手动触发：`python src/memory.py consolidate`

### 3. 记忆质量
- 优先标记为 Fact 的内容
- Belief 需要定期验证
- 低置信度的 Belief 会自动衰减

---

## 🔮 扩展能力

### 可选集成

1. **lancedb-pro 检索增强**
   - 在增强模式基础上
   - 添加 Cross-Encoder Rerank
   - 进一步提升检索精度到 90%+

2. **OpenClaw 插件**
   - 作为 OpenClaw 的记忆插件
   - 自动捕获对话
   - 自动回忆相关记忆

3. **多设备同步**
   - 通过 Git 同步 memory.db
   - 或使用 Syncthing 等工具

---

## 📝 总结

**个人版方案 = openclaw_memory_supersystem-v1.0**

**核心优势**:
1. 成熟稳定（7385 行代码）
2. 纯本地化（SQLite）
3. 低硬件需求（纯 CPU 可用）
4. 独特价值（7 阶段 Consolidation + Fact/Belief 区分）

**推荐配置**: 轻量模式（满足核心需求）

**可选升级**: 增强模式（追求高精度）
